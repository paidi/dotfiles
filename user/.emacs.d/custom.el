;; This file is written to by Emacs' own Customize UI (`M-x customize`,
;; `custom-set-variables`/`custom-set-faces`). Keep it separate from
;; init.el so Customize can rewrite it freely without touching hand-written
;; config. It's expected to be empty until you use `M-x customize`.
