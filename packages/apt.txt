# Minimal base packages installed with `apt-get install` on Ubuntu, just
# enough to bootstrap Homebrew (Linuxbrew) and zsh. Everything else is
# installed via Homebrew from packages/brew.txt, same as on macOS.
# One package per line, # lines are comments.

build-essential
procps
curl
file
git
zsh
